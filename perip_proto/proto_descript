Данный файл является описанием протокола управления UART по ethernet.

Протокол прикладного уровня модели OSI. 
Может применяться не только в поверх TCP/IP. 
Возможно использование любого протокола поверх на нижнем уровне.

Описание протокола.

1.  
    Заголовок содержит 2 байта:
       [1] - номер интерфейса (задается в процессе инициализации протокола).
       [2] - тип пакета (0x01 - конфигурационный пакет, 0x02 - пакет с данными).

    Конфигурационный пакет имеет разную длину для различных переферийных интерфейсов:
    В данном случае для UART переферии он задается следующими значениями.
    Смещение   Длина(байт)   Название в структуре       Описание
       [4]        4           baud_rate              скорость интерфейса 
       [8]        1           data_bit               длина данных интерфейса [7, 8, 9]
       [9]        1           stop_bit                 длина стоп бита  
       [10]       1           parity                   контроль четности 
       [11]       1           parity_type              тип контроля четности (четный, нечетный)
       [12]       1           port_num                используемые выходные пины для данного интерфейса


    Протокол состоит из 2-х типов пакетов

    Заголовок содержит 2 байта:
       [1] - номер интерфейса (задается в процессе инициализации протокола).
       [2] - тип пакета (0x01 - конфигурационный пакет, 0x02 - пакет с данными).

    Конфигурационный пакет имеет разную длину для различных переферийных интерфейсов:
    В данном случае для UART переферии он задается следующими значениями.
    Смещение   Длина(байт)   Название в структуре       Описание
       [4]        4           baud_rate              скорость интерфейса 
       [8]        1           data_bit               длина данных интерфейса [7, 8, 9]
       [9]        1           stop_bit                 длина стоп бита  
       [10]       1           parity                   контроль четности 
       [11]       1           parity_type              тип контроля четности (четный, нечетный)
       [12]       1           port_num                используемые выходные пины для данного интерфейса

    Ответ на конфигурационный пакет.
    2 байта 
       [1] - номер интерфейса (задается в процессе инициализации протокола).
       [2] - результат конфигурации (configOk = 0x02, configErr = 0x01).

    Пакет данных имеет разную длину имеет следующую структуру:
    В данном случае для UART переферии он задается следующими значениями.
    Смещение   Длина(байт)   Название в структуре       Описание
       [0]        2             data_len               длина данных 
       [1]       data_len       data                     данные
       
    Ответ на пакет данных.
    2 байта 
       [1] - номер интерфейса (задается в процессе инициализации протокола).
       [2] - результат отправки данных (configOk = 0x02, configErr = 0x01).
